#include <bits/stdc++.h>

using namespace std;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(0);
  int t, l, r;
  cin >> t;
  while (t--) {
    cin >> l >> r;
    cout << l << ' ' << l * 2 << endl;
  }
  return 0;
}
